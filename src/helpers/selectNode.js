/**
 * Function that gets an HTML node via its selector.
 * @param {string} selector - Selector that identifies the desired element.
 */
export const $ = (selector) => document.querySelector(selector);
